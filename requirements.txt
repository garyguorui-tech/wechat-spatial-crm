# ===== RPA 数据提取（rpa_extractor.py）=====
uiautomation>=2.0.20      # Windows UI 自动化（控件树定位，非图像识别）
pyperclip>=1.8.2          # 剪贴板读写（文本提取兜底方案）

# ===== 前端看板（crm_dashboard.py）=====
streamlit>=1.35.0         # Web 看板框架
folium>=0.16.0            # 交互式地图
streamlit-folium>=0.20.0  # Folium 与 Streamlit 的桥接组件
pandas>=2.0.0             # 客户管理可编辑表格（st.data_editor）

# ===== 数据导出（export_data.py）=====
openpyxl>=3.1.0           # 导出 Excel(.xlsx)
